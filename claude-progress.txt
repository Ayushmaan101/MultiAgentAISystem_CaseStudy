PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: Single routing layer. Ollama phi3.5 classifies, Python dispatches to sub-agents directly. Groq coordinator LLM removed. Clean architecture.
  - agents/coordinator.py: Pure Python — no Agent/LLM. Ollama phi3.5 classifies query (CALCULATOR/SEARCH/RAG),
    Python dispatches directly to calculator_agent.run(), web_search_agent.run(), or rag_agent.run().
    Keyword fallback (_keyword_classify) used when Ollama unreachable.
  - app.py: Single route_query tool wraps run_coordinator(). Coordinator Agent (openai/gpt-oss-20b) has
    only one tool (route_query) — always calls it, returns result verbatim.
  - All agents use openai/gpt-oss-20b (Groq). OpenAI-architecture model consistently emits JSON
    function-call format. llama-3.3-70b-versatile and llama-3.1-8b-instant both emit Hermes XML format
    for "known" queries (rejected by Groq validation). gpt-oss-20b never uses Hermes XML.
  - llm_client.py: Removed get_router_model(). get_coordinator_model/get_fallback_model/get_precise_model
    all resolve to openai/gpt-oss-20b via GROQ_COORDINATOR_MODEL/GROQ_MODEL/GROQ_PRECISE_MODEL.
  - config.py: Removed GROQ_ROUTER_MODEL. All three model constants = openai/gpt-oss-20b.
  TEST RESULTS (5/5 PASS — via AgentOS API, openai/gpt-oss-20b for all agents):
    TEST 1 CALCULATOR "three dozen eggs / half": route_query called, routed to Calculator, answer=18  PASS
    TEST 2 RAG "retrieval pipeline": route_query called, routed to RAG, chunks shown with source/score  PASS
    TEST 3 SEARCH "Eiffel Tower built": route_query called, routed to Web Search, URLs shown  PASS
    TEST 4 RAG "architecture": route_query called, routed to RAG, architecture.md chunks shown  PASS
    TEST 5 CALCULATOR "sqrt(256)": route_query called, routed to Calculator, result=16.0  PASS
  ARCHITECTURE:
    Coordinator (gpt-oss-20b) -> route_query tool -> run_coordinator() -> Ollama classify ->
    Python dispatch -> sub-agent (gpt-oss-20b) -> tool (document_lookup / safe_calculate / web_search)
  NOTE: Corporate SSL proxy: Both sync and async httpx clients have verify=False. Do NOT remove.
  NOTE: web_search uses direct httpx.Client(verify=False).post() to Tavily — NOT TavilyClient (no SSL bypass).
NEXT IMMEDIATE TASK: Final verification and GitHub push.
