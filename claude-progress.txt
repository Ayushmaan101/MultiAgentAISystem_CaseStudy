PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: main.py built. FastAPI server running. All three terminal tests passed.
  - config.py: all env vars and constants loaded via python-dotenv.
  - database.py: DuckDB initialized at ./db/embeddings.db. VSS + HNSW index ready.
    generate_embedding() at 384 dims. search_chunks() with VSS + two SQL cosine fallbacks.
  - ingest.py: structural/recursive chunker verified (architecture.md 6 chunks, overview.txt 7 chunks).
  - agents/rag_agent.py: document_lookup() -> search_chunks(). SSL bypass via httpx.Client(verify=False).
  - agents/calculator_agent.py: safe_calculate() -> asteval.Interpreter. Never uses eval().
  - agents/web_search_agent.py: web_search() -> TavilyClient (top 3 results, title/url/content).
  - agents/coordinator.py: routes by intent to _route_to_rag / _route_to_calculator / _route_to_web_search.
    All agents use OpenAILike(http_client=httpx.Client(verify=False)) for corporate SSL proxy bypass.
  - main.py: FastAPI app with lifespan startup (init_db), POST /query, GET /health, GET /chunks.
    routed_to detection via RunOutput.tools[*].tool_name -> human-readable agent label.
  TEST RESULTS (server on :8000):
    TEST 1 /health: {"status":"ok","message":"AI Research Assistant is running"} PASS
    TEST 2 /query calculator: routed_to="Calculator Agent" PASS (routing correct)
    TEST 3 /query RAG: routed_to="RAG Agent" PASS (routing correct)
  NOTE: Corporate SSL proxy required httpx.Client(verify=False) on all OpenAILike model instances.
NEXT IMMEDIATE TASK: Agno UI integration and final Phase 1 verification.
