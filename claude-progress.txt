PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: Full architecture restructure complete. Ollama classifies and rewrites. Python calls tools
  directly. qwen3-32b synthesizes only. Raw tool results in dropdown. Clean answers in chat.
  - agents/coordinator.py: Ollama phi3.5 does TWO jobs in ONE call — classify (RAG/CALCULATOR/SEARCH)
    AND rewrite query for the target tool. Python dispatches directly to safe_calculate() /
    web_search() / database.search_chunks(). qwen3-32b called via direct httpx POST to Groq API for
    final answer synthesis only. No Agno Agent inside coordinator.py at all.
  - agents/rag_agent.py: Pure Python function document_lookup() only. No Agno Agent.
  - agents/calculator_agent.py: Pure Python function safe_calculate() only. No Agno Agent.
  - agents/web_search_agent.py: Pure Python function web_search() only. No Agno Agent.
    Still uses httpx.Client(verify=False) for Tavily — NOT TavilyClient (no SSL bypass).
  - app.py: Single coordinator Agent (gpt-oss-20b for reliable JSON tool calls) registered in AgentOS.
    route_query tool returns structured string with === RAW TOOL RESULT === and === SYNTHESIZED ANSWER ===
    sections. Coordinator Agent extracts and returns only SYNTHESIZED ANSWER to chat.
    Raw tool result visible in AgentOS tools dropdown.
  - config.py: QWEN_MODEL = "qwen/qwen3-32b" (synthesis), GROQ_MODEL = "openai/gpt-oss-20b" (shell).
  - llm_client.py: get_synthesis_model() returns gpt-oss-20b for coordinator shell (via OpenAILike).
    qwen3-32b synthesis done directly via httpx in coordinator.py to avoid Agno overhead.
  - main.py: Updated QueryResponse with rewritten_query, raw_tool_result, final_answer fields.
  KEY LESSON: qwen3-32b answers "known" queries (math facts, common knowledge) from memory without
    calling tools — same Hermes XML problem as llama models. gpt-oss-20b is the only Groq model that
    consistently emits JSON function calls for the coordinator shell.
    qwen3-32b works well for synthesis (httpx call, no tool calling involved).
  NOTE: Corporate SSL proxy: Both sync and async httpx clients have verify=False. Do NOT remove.
  NOTE: web_search uses direct httpx.Client(verify=False).post() to Tavily — NOT TavilyClient.
  TEST RESULTS (5/5 PASS — AgentOS API):
    TEST 1 CALCULATOR "three dozen eggs / half":
      rewritten_query=(3 * 12) / 2 | raw=Expression: (3 * 12) / 2 / Result: 18.0 | answer=18 eggs  PASS
    TEST 2 RAG "embedding model in this system":
      rewritten_query=embedding model utilized within the current system | raw=BAAI/bge-small-en-v1.5
      chunk from overview.txt | answer cited Chunk 2, overview.txt  PASS
    TEST 3 SEARCH "Eiffel Tower built":
      rewritten_query=When was the construction of the Eiffel Tower completed? | raw=3 results with URLs
      | answer=1887-1889 with Wikipedia URL  PASS
    TEST 4 RAG "architecture":
      rewritten_query=system architecture components and design | raw=architecture.md chunks shown
      | answer synthesized from Chunk 2 architecture.md  PASS
    TEST 5 CALCULATOR "15 percent of 240":
      rewritten_query=0.15 * 240 | raw=Expression: 0.15 * 240 / Result: 36.0 | answer=36  PASS
  ARCHITECTURE FLOW:
    User query
      -> Coordinator Agent (gpt-oss-20b) calls route_query()
        -> run_coordinator(): Ollama phi3.5 classify+rewrite (one call)
          -> Python dispatch: safe_calculate() / web_search() / search_chunks()
            -> qwen3-32b synthesis (httpx POST to Groq /chat/completions)
              -> route_query returns structured string (raw + synthesized)
                -> AgentOS: [dropdown]=raw tool result, [chat]=synthesized answer
NEXT IMMEDIATE TASK: GitHub push and final submission.
