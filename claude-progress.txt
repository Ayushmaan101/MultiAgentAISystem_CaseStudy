PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: OpenRouter restored as primary. Groq fallback added.
  Deterministic coordinator routing documented as intentional design decision.
  All sub-agents returning real tool results via OpenRouter (with Groq fallback).
  - config.py: OpenRouter (primary) + Groq (fallback) constants. LLM_MODEL from .env.
  - llm_client.py: get_model() -> OpenRouter OpenAILike. get_fallback_model() -> native Groq class.
    _is_retryable(), _is_error_response() detect transient provider failures and LLM error responses.
    Logs "Using OpenRouter" at agent construction; "Falling back to Groq" when fallback triggers.
  - agents/rag_agent.py: uses get_model() (OpenRouter primary via OpenAILike).
  - agents/calculator_agent.py: uses get_model() (OpenRouter primary via OpenAILike).
  - agents/web_search_agent.py: uses get_model() (OpenRouter primary via OpenAILike).
  - agents/coordinator.py: run_coordinator() uses deterministic keyword regex routing.
    Routing functions (_run_with_fallback) implement 3-tier fallback:
      Tier 1: OpenRouter LLM (primary)
      Tier 2: Groq LLM via native GroqModel (better tool-call schema handling)
      Tier 3: Direct Python tool call (guarantees real result when both LLMs fail)
    All agents use httpx.Client(verify=False) via llm_client.py for corporate SSL proxy bypass.
  - main.py: calls run_coordinator(); 3 endpoints: POST /query, GET /health, GET /chunks.
  TEST RESULTS (server on :8000):
    TEST 1 /health: {"status":"ok","message":"AI Research Assistant is running"} PASS
    TEST 2 /query calculator: routed_to="Calculator Agent" response="...144/12+37...49.0" PASS
      Server log: "Using OpenRouter" -> 429 -> "OpenRouter error" -> "Falling back to Groq" -> 200 OK
    TEST 3 /query RAG: routed_to="RAG Agent" response=actual chunk text from DuckDB PASS
      Server log: "Using OpenRouter" -> 429 -> "OpenRouter error" -> "Falling back to Groq" ->
                  Groq tool name mismatch -> "Groq fallback failed...calling document_lookup directly"
  NOTE: OpenRouter free-tier rate-limited (429). When quota resets/paid credits added, OpenRouter
        will be used directly. Groq fallback activates automatically on any retryable error.
  NOTE: Corporate SSL proxy requires httpx.Client(verify=False) (managed in llm_client.py).
NEXT IMMEDIATE TASK: Agno UI integration and Phase 1 completion.
