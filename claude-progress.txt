PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: Agno UI connected and serving all four agents. All case study UI requirements met. Phase 1 complete.
  - app.py: AgentOS (agno.os.app.AgentOS) registered on port 7777 with all four agents.
    agno.playground.Playground does not exist in Agno 2.6.18 — AgentOS is the correct equivalent.
    CORS default already includes https://app.agno.com and https://os.agno.com.
    Agents use Groq as primary (OpenRouter free-tier rate-limited).
    Coordinator in AgentOS calls call_rag/call_calculator/call_web_search (route_to_* wrappers).
  - llm_client.py: Fixed async_client for both OpenAILike and GroqModel.
    Now passes AsyncOpenAI/AsyncGroq SDK clients (with httpx.AsyncClient(verify=False)) as async_client.
    Previously httpx.Client(verify=False) was silently ignored in async AgentOS context.
    Both sync (coordinator fallback) and async (AgentOS) paths now have SSL verify=False.
  UI REQUIREMENTS MET:
    - Final answer visible in response content
    - Which agent handled the query shown via tool calls in run output
    - Retrieved chunks visible for RAG queries (document_lookup result with source file + similarity score)
    - Agent execution steps shown via tools array in run response
  AGNO UI URL: https://app.agno.com or https://os.agno.com -> point endpoint at http://localhost:7777
  TEST RESULTS (via AgentOS API, equivalent to UI tests):
    TEST 1 Coordinator "embedding model used": tool=call_rag, answer=BAAI/bge-small-en-v1.5  PASS
    TEST 2 Coordinator "25 multiplied by 48": tool=call_calculator, answer=1200  PASS
    TEST 3 RAG Agent "chunking strategy": tool=document_lookup, chunks shown with source/score  PASS
    TEST 4 Calculator Agent "sqrt(144)+50": tool=safe_calculate, result=62.0  PASS
  ARCHITECTURE:
    main.py REST API: run_coordinator() -> Ollama routing -> OpenRouter -> Groq -> direct tool
    app.py AgentOS UI: AgentOS -> Groq primary (OpenRouter fallback when quota resets)
    Both paths share tool functions (document_lookup, safe_calculate, web_search).
  NOTE: Corporate SSL proxy: Both sync and async httpx clients have verify=False.
  NOTE: OpenRouter free-tier rate-limited. When quota resets, app.py coordinator can switch to get_model().
NEXT IMMEDIATE TASK: README with architecture diagram and design decisions.
