PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: All four agents built and syntax-verified. Coordinator, RAG, Calculator, Web Search agents complete.
  - config.py: all env vars and constants loaded via python-dotenv.
  - database.py: DuckDB initialized at ./db/embeddings.db. VSS extension loaded.
    HNSW index created (hnsw_enable_experimental_persistence=true). document_chunks table schema in place.
    generate_embedding() verified at 384 dims. search_chunks() has VSS primary path + list_cosine_similarity
    fallback + manual LIST_DOT_PRODUCT last-resort fallback.
  - ingest.py: structural/recursive chunker (headers > double-newlines > single-newlines > char limit).
    File parsers for .pdf (pypdf), .md (raw text), .txt (raw text). CHUNK_OVERLAP context prepended per chunk.
    MD5-based chunk IDs, INSERT OR REPLACE upsert to document_chunks. ingest_directory() verified.
  - agents/rag_agent.py: Agno Agent using document_lookup() tool -> search_chunks() from database.py.
    System prompt enforces always calling tool before answering; shows retrieved context in response.
  - agents/calculator_agent.py: Agno Agent using safe_calculate() tool -> asteval Interpreter.
    Never uses Python eval(). Returns expression + result or error message.
  - agents/web_search_agent.py: Agno Agent using web_search() tool -> TavilyClient (top 3 results).
    Returns title, url, content snippet per result. Source citation enforced in system prompt.
  - agents/coordinator.py: Agno Coordinator with three routing tools (_route_to_rag, _route_to_calculator,
    _route_to_web_search). Routes by intent: docs -> RAG, math -> Calculator, external -> Web Search.
    Uses OpenRouter (LLM_MODEL from config) via OpenAILike backend.
NEXT IMMEDIATE TASK: Build main.py FastAPI entry point and run end-to-end terminal test.
