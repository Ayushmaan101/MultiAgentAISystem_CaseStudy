PROMPT OBJECTIVE: Build Phase 1 baseline for AI Research Assistant (Project Delphi / Case Study 2).
CURRENT STATE: Sub-agent tool execution fixed and verified. All agents returning real results.
  - config.py: all env vars loaded. GROQ_API_KEY, GROQ_BASE_URL, GROQ_MODEL added.
  - database.py: DuckDB initialized at ./db/embeddings.db. VSS + HNSW index ready.
    generate_embedding() at 384 dims. search_chunks() with VSS + two SQL cosine fallbacks.
  - ingest.py: structural/recursive chunker verified (architecture.md 6 chunks, overview.txt 7 chunks).
  - agents/rag_agent.py: document_lookup() -> search_chunks(). Uses agno.models.groq.Groq.
  - agents/calculator_agent.py: safe_calculate() -> asteval.Interpreter. Never uses eval().
  - agents/web_search_agent.py: web_search() -> TavilyClient (top 3 results, title/url/content).
  - agents/coordinator.py: run_coordinator() uses keyword regex routing to sub-agents.
    All agents use Groq(http_client=httpx.Client(verify=False)) for corporate SSL proxy bypass.
    Model: meta-llama/llama-4-scout-17b-16e-instruct on Groq (free tier).
  - main.py: FastAPI app with lifespan startup (init_db), POST /query, GET /health, GET /chunks.
    Calls run_coordinator() which keyword-routes and invokes the correct sub-agent.
  TEST RESULTS (server on :8000):
    TEST 1 /health: {"status":"ok","message":"AI Research Assistant is running"} PASS
    TEST 2 /query calculator: routed_to="Calculator Agent" response contains "49" PASS
    TEST 3 /query RAG: routed_to="RAG Agent" response contains actual chunk text PASS
  NOTE: Corporate SSL proxy required httpx.Client(verify=False) on all Groq model instances.
  NOTE: OpenRouter free-tier models exhausted/rate-limited; switched to Groq with native Agno Groq class.
  NOTE: Groq free-tier Llama 3.3/4 Scout have XML tool-call format issues; coordinator uses keyword routing.
NEXT IMMEDIATE TASK: Agno UI integration and Phase 1 completion.
